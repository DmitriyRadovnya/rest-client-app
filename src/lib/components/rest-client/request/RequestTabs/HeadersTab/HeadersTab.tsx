import React, { FC } from 'react';
import { Stack } from '@mui/material';

interface HeadersTabProps {
  headers: HeaderRow[];
}

const HeadersTab: FC<HeadersTabProps> = ({ headers }) => {
  return <Stack></Stack>;
};

export default HeadersTab;
